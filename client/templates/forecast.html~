<template name="forecast">
<tr>
	<td>
	The forcast is: {{getForecastDate(forecast.data.currently.time)}}
	</td>
	<td>
		{{getDailyForecast}}
	</td>
	</tr>
	{{#each forecast.data.hourly.data}}
	{{> hourlyForecast}}
	{{/each}}
</template>
